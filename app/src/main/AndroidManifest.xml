<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.landergame">
    <!--Licence GPL-->
    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_game"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_game"
        android:supportsRtl="true"
        android:theme="@style/Theme.LanderGame">

        <activity
            android:name=".SplashScreenActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.LanderGame.Fullscreen" />
        <activity
            android:name=".GameOverActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.LanderGame.Fullscreen" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.LanderGame.Fullscreen"></activity>


    </application>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>